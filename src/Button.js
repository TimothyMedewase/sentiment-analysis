import React from "react";

const Button = () => {
  return (
    <div>
      <button type="button" className="btn btn-primary">
        Analyze
      </button>
    </div>
  );
};

export default Button;
